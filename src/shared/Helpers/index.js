import {
  setPageTitle,
  groupArrayItemsInArrays,
  setFieldClassName,
  clearFieldErrorMessage,
} from './General';

import {
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
} from './Formatters';

export {
  // General
  setPageTitle,
  groupArrayItemsInArrays,
  setFieldClassName,
  clearFieldErrorMessage,

  // Formatters
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
};
